# Environment example for node-starter-template-v2
# Copy this file to .env and fill the values before running the app.

# NODE environment: development | test | production
NODE_ENV=development

# HTTP port
PORT=8082

# MongoDB connection (no trailing -test; test runner will append '-test' automatically when NODE_ENV=test)
MONGODB_URL=mongodb://localhost:27017/bneighbourly

# Twilio (SMS) credentials
TWILIO_PHONE=+15551234567
TWILIO_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# AWS S3 credentials used by file upload service
AWS_S3_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_S3_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
AWS_S3_REGION=us-east-1
AWS_S3_BUCKET=your-s3-bucket-name

# Paystack (optional) — service references config.paystack.secretKey; add if you use Paystack
PAYSTACK_SECRET_KEY=your_paystack_secret_key

# Notes:
# - This project also uses Firebase Admin which expects a service account JSON file at the repo root:
#   firebase-service-secret.json
#   This file is not stored in environment variables here. Place the JSON file in the repo root and ensure it's ignored by git.
# - Additional environment variables may be required if you integrate more microservices; update this file accordingly.
